{
  "name": "twinkle-labs-design",
  "version": "0.1.0",
  "private": true,
  "description": "Aurora Ledger — Twinkle AI Labs 디자인 시스템. design.twinklelabs.kr",
  "scripts": {
    "dev": "lsof -ti:3946 | xargs kill -9 2>/dev/null || true && next dev -p ${PORT:-3946}",
    "build": "DESIGN_EXPORT=1 next build && rm -rf out && mv .next-export out",
    "preview": "npm run build && npx --yes serve out -l 3947"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.12.0",
    "next": "^15.5.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-redux": "^9.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.7.0"
  }
}
